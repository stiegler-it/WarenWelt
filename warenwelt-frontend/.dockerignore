# Git
.git
.gitignore

# Node modules
node_modules/

# Build output (this will be created inside the Docker build process)
dist/
.output/ # Nuxt.js specific, good to have if switching later

# Vue-specific files not needed in final image
# (if any, usually handled by build process)

# IDE and OS specific
.idea/
.vscode/
*.DS_Store

# Log files
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Local environment files (template is fine, .env itself should not be committed)
.env
.env.*
!.env.example # Keep example files

# Test reports
coverage/
test-results/

# Docker related files (if any inside frontend that are not needed in image)
# Dockerfile (already processed)
# .dockerignore (this file)
# nginx.conf (copied during build)
