# Git
.git
.gitignore

# Python virtual environment
.venv
venv/
env/
ENV/

# Python bytecode
__pycache__/
*.pyc
*.pyo
*.pyd

# SQLite database file (if created locally during development)
test.db
*.sqlite3

# IDE and OS specific
.idea/
.vscode/
*.DS_Store

# Docker related files (if any inside backend that are not needed in image)
# Dockerfile (already processed)
# .dockerignore (this file)

# Alembic local env files (if any specific to local machine)
# alembic.ini is needed, env.py is needed

# Test files and reports
tests/
.pytest_cache/
htmlcov/
.coverage

# Local environment files (template is fine)
.env

# Wheels built locally if not part of a multi-stage build strategy
# wheels/ (handled by multi-stage build)
